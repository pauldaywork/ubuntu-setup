# TODO
- make first default workspace called general
- turn animations on
- create Code folder in home and set that to the default starting location for tmux
- any config around needs to be changed now that mako isn't used.
- make sure mako isn't installed and remove it before running install script. add this check to doctor.sh too
- make the tmux bar less bright

# Research
- how to I slience or manage notifications in dms?
- describe best workflow and shortcuts to use for basic tmux useage with coding

# Unpack With User
- sync tasks between computers
- sudo apt update && sudo apt install -y build-essential libudev-dev (needed for rust)
- share important files via network or load important files for projects onto local hdd
- remove brightnessctl  mako-notifier  playerctl since it was added when I re ran install on main pc. so that must mean it was never installed or needed in the first place?
