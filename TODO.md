# TODO

# check completed
- pull in VS Code extenshion for .kdl files
- block or slience all alerts like app updates via the default DMS notification service

# Extra
- turn animations on

# Research
- describe best workflow and shortcuts to use for basic tmux useage with coding

# Unpack With User
- sync tasks between computers
- sudo apt update && sudo apt install -y build-essential libudev-dev (needed for rust)
- share important files via network or load important files for projects onto local hdd
- remove brightnessctl playerctl since it was added when I re ran install on main pc. so that must mean it was never installed or needed in the first place? (mako-notifier resolved: it was a standalone manual install from 2026-05-07, predating DMS's built-in notifications; install.sh now strips it and it's been removed from this machine)
