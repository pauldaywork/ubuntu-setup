# README

## Things to add
- sudo apt update && sudo apt install -y build-essential libudev-dev (needed for rust)
- sudo apt install zenity (for workspace create wortflow and any other inputs needed in desktop ui)  